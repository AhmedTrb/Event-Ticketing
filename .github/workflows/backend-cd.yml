name: Backend CD

on:
  push:
    branches: [ "main" ]
    paths:
      - 'backend/**'

jobs:
  build-and-push-images:
    runs-on: ubuntu-latest

    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
      # Only run if secrets are available
      if: "${{ secrets.DOCKERHUB_USERNAME != '' }}"
      
    # Example step for Ticketing API
    - name: Build and push Ticketing API
        # context: ./backend/ticketing-api
        # file: ./backend/ticketing-api/Dockerfile
        # push: true
        # tags: user/ticketing-api:latest
      run: echo "Uncomment and configure Docker build steps once Dockerfiles are created."

    # Example step for Registration Worker
    - name: Build and push Registration Worker
      run: echo "Uncomment and configure Docker build steps once Dockerfiles are created."
